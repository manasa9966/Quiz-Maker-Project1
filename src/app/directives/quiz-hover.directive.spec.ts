import { QuizHoverDirective } from './quiz-hover.directive';

describe('QuizHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new QuizHoverDirective();
    expect(directive).toBeTruthy();
  });
});
